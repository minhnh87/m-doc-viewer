/**
 * @typedef {Object} FileEntry
 * @property {string} name - File name
 * @property {string} path - Relative path (project) or absolute path (external)
 * @property {string} folder - Parent folder relative path or '.' for root
 * @property {string} fullPath - Absolute filesystem path
 * @property {'markdown'|'drawio'|'mermaid'} type - File type
 * @property {boolean} [isExternal] - True if from external folder
 */

export {};
